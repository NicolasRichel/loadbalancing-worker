const NAME = 'Destroy';
const DESCRIPTION = 'should terminate worker execution';

export function test() {
  return {
    testName: NAME,
    description: DESCRIPTION,
    message: '',
    success: false
  };
};
